# Backend API base URL (the NexusDash backend).
VITE_API_URL="http://localhost:5062"

# Use the built-in mock API instead of proxying to the backend.
# Set to "false" to talk to the real backend.
USE_MOCK="false"

# Port the dev server listens on (defaults to 3000).
PORT="3000"
